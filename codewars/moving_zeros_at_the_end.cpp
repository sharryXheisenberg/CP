#include<bits/stdc++.h>
using namespace std;

vector<int> move_zeroes(const vector<int>& input) {
  int n = input.size();
  for(int i=0;i<n;i++){
    if(input[i]==0){
        
    }
  }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);


}